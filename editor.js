requireLogin();const editId=new URLSearchParams(location.search).get('edit');if(editId){const p=get('posts').find(x=>x.id==editId);if(p){$('formTitle').innerText='Edit Blog';$('title').value=p.title;$('category').value=p.category;$('image').value=p.image||'';$('content').value=p.content}}
function savePost(){const u=user(),title=$('title').value.trim(),category=$('category').value,image=$('image').value.trim(),content=$('content').value.trim();if(!title||!content)return alert('Title and content required');let posts=get('posts');if(editId){let p=posts.find(x=>x.id==editId);if(p.email!==u.email)return alert('Not allowed');Object.assign(p,{title,category,image,content})}else posts.unshift({id:Date.now(),title,category,image,content,author:u.name,email:u.email,likes:0,created:new Date().toLocaleString()});set('posts',posts);location.href='index.html'}
