const id=Number(new URLSearchParams(location.search).get('id'));function render(){const p=get('posts').find(x=>x.id===id);if(!p)return $('postDetail').innerHTML='<h1>Post not found</h1>';$('postDetail').innerHTML=`${p.image?`<img src="${p.image}" style="width:100%;border-radius:20px">`:''}<span class="tag">${p.category}</span><h1>${p.title}</h1><p>${p.content}</p><small>By ${p.author} • ❤️ ${p.likes||0}</small>`;$('comments').innerHTML=get('comments').filter(c=>c.postId===id).map(c=>`<div class="comment"><b>${c.name}</b><p>${c.text}</p><small>${c.time}</small></div>`).join('')||'<p>No comments yet</p>'}
function addComment(){requireLogin();const text=$('commentText').value.trim();if(!text)return alert('Write comment');let c=get('comments');c.push({postId:id,text,name:user().name,time:new Date().toLocaleString()});set('comments',c);$('commentText').value='';render()}render();
