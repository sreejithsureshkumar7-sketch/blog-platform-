function loadPosts(){const q=($('search')?.value||'').toLowerCase();const cat=$('category')?.value||'';let posts=get('posts').filter(p=>(p.title.toLowerCase().includes(q)||p.content.toLowerCase().includes(q))&&(!cat||p.category===cat));$('posts').innerHTML=posts.map(p=>`<article class="post">${p.image?`<img src="${p.image}">`:''}<span class="tag">${p.category}</span><h2>${p.title}</h2><p>${p.content.slice(0,110)}...</p><small>By ${p.author} • ${p.created}</small><div class="actions"><button onclick="openPost(${p.id})">Read</button><button onclick="likePost(${p.id})">❤️ ${p.likes||0}</button><button onclick="sharePost(${p.id})">Share</button>${user()?.email===p.email?`<button onclick="editPost(${p.id})">Edit</button><button class="danger" onclick="deletePost(${p.id})">Delete</button>`:''}</div></article>`).join('')||'<p>No blogs found</p>'}
function openPost(id){location.href='post.html?id='+id}function likePost(id){let posts=get('posts');let p=posts.find(x=>x.id===id);p.likes=(p.likes||0)+1;set('posts',posts);loadPosts()}function sharePost(id){navigator.clipboard?.writeText(location.origin+location.pathname.replace('index.html','')+'post.html?id='+id);alert('Post link copied')}function editPost(id){location.href='create-post.html?edit='+id}function deletePost(id){if(confirm('Delete this post?')){set('posts',get('posts').filter(p=>p.id!==id));loadPosts()}}loadPosts();
