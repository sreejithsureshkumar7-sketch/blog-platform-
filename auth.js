function register(){const name=$('name').value.trim(),email=$('email').value.trim(),password=$('password').value.trim();if(!name||!email||!password)return alert('Fill all details');let users=get('users');if(users.find(u=>u.email===email))return alert('Email already registered');users.push({name,email,password});set('users',users);alert('Registered successfully');location.href='login.html'}
function login(){const email=$('email').value.trim(),password=$('password').value.trim();const u=get('users').find(x=>x.email===email&&x.password===password);if(!u)return alert('Wrong email or password');localStorage.setItem('currentUser',JSON.stringify(u));location.href='index.html'}
